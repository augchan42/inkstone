.vercel
.playwright-mcp/
slide*.png
sample-*.jpeg
*-check*.png

# macOS
.DS_Store
**/.DS_Store

# Skill install artifacts (npx skills add)
# Running the CLI inside this repo replaces skills/<name>/ with symlinks into
# .agents/ — restore with: find skills -maxdepth 1 -type l -delete && git checkout -- skills/
.agents/
skills-lock.json
agent/
.claude/skills/

# Personal voice profiles (voice-calibration output) — keep local, not published
skills/voice-calibration/profiles/*
!skills/voice-calibration/profiles/.gitkeep
